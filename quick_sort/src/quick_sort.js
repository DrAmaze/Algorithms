// Quick sort has average case time complexity O(nlogn), but worst
// case O(n**2).

// Not in-place. Uses O(n) memory.
Array.prototype.sort1 = function() {

};

// In-place.
Array.prototype.sort2 = function(start = 0, length = this.length) {

};

Array.prototype.partition = function(start, length) {

};
